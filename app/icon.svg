<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Quatro casas: o menor tabuleiro que ainda é um tabuleiro. As duas cores
       são `metodo-cheio` e `papel` da identidade; escritas em hexadecimal
       porque um favicon é lido fora do documento, sem acesso a `var()`.
       `lib/tema/guardas.test.ts` cobra que continuem iguais aos tokens. -->
  <rect width="32" height="32" rx="6" fill="#15552e" />
  <g fill="#ebf0ec">
    <rect x="6" y="6" width="10" height="10" />
    <rect x="16" y="16" width="10" height="10" />
  </g>
</svg>
